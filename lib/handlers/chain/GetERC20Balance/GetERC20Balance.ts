import WalletConnect from "@walletconnect/client"

const GetERC20Balance = async (connector: WalletConnect, address: string) => {
  // TODO
  // const { initialize } = await import("@lib/useProvider")
  // const { ERC20 } = await import("@lib/initProvider")
  // const { provider } = await initialize(connector)
  // const contract = ERC20(provider)
  // try {
  //   const call = await contract.balanceOf(address)
  //   return [contract, call]
  // } catch (err) {
  //   throw err
  // }
}

export default GetERC20Balance
